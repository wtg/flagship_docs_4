<% featured.each do |category, docs| %>
  <% if !docs.empty? and !category.nil?%>
    <div class="container">
      <% if category.is_root? %>
        <h4> <%= category.name %> </h4>
      <% else %>
        <h4> <%= "#{category.root.name} - #{category.name}" %> </h4> 
      <% end %>
      <% docs.each do |doc| %>
        <%= render partial: "documents/document_list_item", locals: {doc: doc} %>
      <% end %>
    </div>
  <% end %>
<% end %>